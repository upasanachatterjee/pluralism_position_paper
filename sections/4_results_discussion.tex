\section{Results}
\label{sec:results}

\begin{figure*}
    \begin{minipage}{0.5\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/byline_balanced.png}
        \subcaption{Results for byline manipulation.}
        \label{fig:byline_balanced}
    \end{minipage}
    \begin{minipage}{0.5\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/register_balanced.png}
        \subcaption{Results for register manipulation.}
        \label{fig:register_balanced}
    \end{minipage}
    \caption{Expertise and rhetorical authority manipulation. Results are shown for the balanced conditions, where the number of passages supporting each answer is equal. The x-axis shows the total number of evidence passages. The y-axis shows movement relative to the unperturbed baseline.}
    \label{fig:expertise_results}
\end{figure*}

Figure~\ref{fig:expertise_results} shows results for the first experiment, which manipulates expertise signals. Models privilege expertise inconsistently when it is relevant to the question, and the pattern depends on model size. The smaller \texttt{Llama-3.1-8B} model privileges expert bylines for both epistemic and aleatoric questions. This effect grows as more passages support each answer, and it is stronger for the epistemic questions. The larger \texttt{Llama-3.3-70B} model shows no significant movement on the byline perturbation.

The academic register shows an unexpected effect reversal. Applied to the \emph{yes} passages, it moves neither model significantly towards \emph{yes}; applied to the \emph{no} passages, it moves both models significantly towards \emph{yes}. The smaller \texttt{Llama-3.1-8B} model behaves the same way under the expert byline, moving significantly towards \emph{yes} when the byline is applied to the \emph{no} passages.

As we discuss in Appendix~\ref{appendix_sub:prompts}, the \emph{yes} passages always appear first, followed by the \emph{no} passages. The lost-in-the-middle effect, compounded by the recency effect, may explain this asymmetry \citep{liu-etal-2024-lost,veseli2025positional,yi-etal-2026-attention}. In the smaller 8B model, many prompts approach the maximum context window (see Appendices~\ref{appendix_sub:models} and~\ref{appendix_sub:prompts} for context windows and prompt sizes), which might be why its effect sizes exceed those of the 70B model. This account does not explain why the register perturbation on the \emph{no} passages moves answers towards \emph{yes}; that result warrants further investigation.


\begin{itemize}
    \item add unbalanced experiment results
    \item add register x byline results <-- these might be more interesting
\end{itemize}

\section{Discussion}

\begin{itemize}
    \item add the note about how ordering has an impact -- this will naturally be a limitation of RAG systems and *may* contribute to a lack of pluralistic groundedness. Would depend on the retrieval layer.
    \item we discuss only one social choice feature "expertise" across a number of questions in multiple domains, larger experiments must be done to see how expertise will interact with other feature (e.g. given an epistemic question about buddhism, should a practising buddhist's opinion be valued more than a christian who is a buddhist scholar? given an aleatoric question about giving candy to kids of particular days should a parent's opinion be more valued than a non-parent's opinion?)
\end{itemize}
